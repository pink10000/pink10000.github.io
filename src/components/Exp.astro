---
import TagItemRow from "./TagItemRow.astro";
---

<!-- Filter & Sort UI
<div class="flex flex-col sm:flex-row sm:items-center justify-between gap-4 mb-6 pb-3 border-b border-light-base01 dark:border-dark-base02 font-mono text-xs select-none">
  <div class="flex flex-wrap items-center gap-1.5 animate-fade-in" id="filterButtonsContainer">
    <span class="text-light-base02 dark:text-dark-base03 mr-1">filter:</span>
    <button 
      type="button"
      class="filter-btn active-filter px-2 py-0.5 rounded font-mono transition-colors border cursor-pointer border-transparent bg-light-base02 dark:bg-dark-base02 text-light-base00 dark:text-dark-base06"
      data-tag="all"
    >
      all
    </button>
    <button 
      type="button"
      class="filter-btn px-2 py-0.5 rounded font-mono transition-colors border cursor-pointer bg-light-base01/40 dark:bg-dark-base01/40 text-light-base05 dark:text-dark-base04 hover:bg-light-base02/45 dark:hover:bg-dark-base02/45 border-light-base02/40 dark:border-dark-base02/40"
      data-tag="distributed systems"
    >
      distributed systems
    </button>
    <button 
      type="button"
      class="filter-btn px-2 py-0.5 rounded font-mono transition-colors border cursor-pointer bg-light-base01/40 dark:bg-dark-base01/40 text-light-base05 dark:text-dark-base04 hover:bg-light-base02/45 dark:hover:bg-dark-base02/45 border-light-base02/40 dark:border-dark-base02/40"
      data-tag="rust"
    >
      rust
    </button>
    <button 
      type="button"
      class="filter-btn px-2 py-0.5 rounded font-mono transition-colors border cursor-pointer bg-light-base01/40 dark:bg-dark-base01/40 text-light-base05 dark:text-dark-base04 hover:bg-light-base02/45 dark:hover:bg-dark-base02/45 border-light-base02/40 dark:border-dark-base02/40"
      data-tag="python"
    >
      python
    </button>
    <button 
      type="button"
      class="filter-btn px-2 py-0.5 rounded font-mono transition-colors border cursor-pointer bg-light-base01/40 dark:bg-dark-base01/40 text-light-base05 dark:text-dark-base04 hover:bg-light-base02/45 dark:hover:bg-dark-base02/45 border-light-base02/40 dark:border-dark-base02/40"
      data-tag="math"
    >
      math
    </button>
    <button 
      type="button"
      class="filter-btn px-2 py-0.5 rounded font-mono transition-colors border cursor-pointer bg-light-base01/40 dark:bg-dark-base01/40 text-light-base05 dark:text-dark-base04 hover:bg-light-base02/45 dark:hover:bg-dark-base02/45 border-light-base02/40 dark:border-dark-base02/40"
      data-tag="lean4"
    >
      lean4
    </button>
  </div>
  
  <div class="flex items-center gap-2">
    <span class="text-light-base02 dark:text-dark-base03">sort:</span>
    <button 
      type="button"
      id="sortToggleBtn" 
      class="px-2 py-0.5 rounded font-mono transition-colors border cursor-pointer bg-light-base01/40 dark:bg-dark-base01/40 text-light-base05 dark:text-dark-base04 hover:bg-light-base02/45 dark:hover:bg-dark-base02/45 border-light-base02/40 dark:border-dark-base02/40"
      data-sort="default"
    >
      default
    </button>
  </div>
</div>
-->
<ul
  id="projectsList"
  class="theme-transition grid grid-cols-[10rem_2px_1fr] md:grid-cols-[10rem_2px_1fr_2px_8rem] gap-x-4 gap-y-4 text-light-base05 mb-4"
>
  <TagItemRow
    title="Tribbler: Distributed Key-Value Store"
    tags={["distributed systems", "rust"]}
  >
    A fault-tolerant, highly available, distributed key-value store built in
    Rust. Utilizes consistent hashing to replicate data across dynamic server
    pools, managed by a consensus-based keeper service. Uses 2-second heartbeats
    for dynamic leader elections and failovers. Features an idempotent,
    append-only log ensuring eventual consistency during network partitions.
    Built for <a
      href="https://cseweb.ucsd.edu/classes/sp26/cse223B-a/tribbler/lab/lab3/index.html"
      target="_blank"
      rel="noopener noreferrer"
      class="text-light-base0B dark:text-dark-base0B underline">CSE 223B</a
    >.
  </TagItemRow>

  <TagItemRow title="SURFStore" tags={["distributed systems", "go"]}>
    A fault-tolerant, asynchronous, distributed, networked database that
    supports file storage and retrieval using the RAFT consensus algorithm as
    part of CSE 224. Used Golang, gRPC, Protobuf, TCP/IP.
  </TagItemRow>

  <TagItemRow
    title="junkfish"
    link="https://github.com/Grace-Note-LLC/distributed-chess"
    tags={["c++", "embedded"]}
  >
    A Chess bot running on low-power recycled IoT microcontroller units on a
    custom chess engine in C++. Uses the minimax algorithm with alpha-beta
    pruning. Won
    <a
      href="https://ieee-qp-fa24.devpost.com/project-gallery"
      target="_blank"
      rel="noopener noreferrer"
      class="text-light-base0B dark:text-dark-base0B underline">1st place</a
    >
    at Fall 2024 UCSD IEEE Quarterly Projects.
  </TagItemRow>

  <TagItemRow
    title="termail"
    link="https://github.com/pink10000/termail"
    tags={["rust", "tui", "wasm"]}
  >
    Architected a TUI email client implementing a plugin system via a custom
    WebAssembly (using the WASI component model) runtime for secure, sandboxed
    extension execution. Engineered a concurrent rendering pipeline to handle
    IMAP/SMTP network I/O asynchronously without blocking UI. Designed a
    modal-centric interface with ratatui, implementing vim-like keybindings for
    keyboard-only navigation. See technical report <a
      href="./papers/termail.pdf"
      target="_blank"
      rel="noopener noreferrer"
      class="text-light-base0B dark:text-dark-base0B underline">here</a
    >.
  </TagItemRow>

  <TagItemRow
    title="vicrds: A Collaborative Text Editor"
    tags={["distributed systems", "rust"]}
  >
    A decentralized, peer-to-peer collaborative text editor written in Rust.
    Implemented a custom CRDT based on a Replicated-Growth Array to guarantee
    strong eventual consistency without a central server. Engineered a
    fault-tolerant backend using gRPC and a gossip-based protocol for dynamic
    peer discovery and failure detection. Features a decentralized tombstone
    compaction algorithm for safe distributed garbage collection across network
    partitions. See technical report <a
      href="./papers/vicrds.pdf"
      target="_blank"
      rel="noopener noreferrer"
      class="text-light-base0B dark:text-dark-base0B underline">here</a
    >.
  </TagItemRow>

  <TagItemRow
    title="Rigid Body Physics Simulation"
    tags={["python", "simulation", "math"]}
  >
    A 3D rigid body physics engine built in Python to model a falling cube
    experiencing gravity and perfectly inelastic collisions. Formulated the
    system's unconstrained motion using the Least Action Principle and
    Lagrangian mechanics. Modeled ground collisions and Coulomb dry friction
    through KKT optimality conditions, solved dynamically via SciPy's SLSQP
    nonlinear optimizer. Visualized the state evolution and rotational
    kinematics using NumPy and Polyscope. Built for <a
      href="#"
      target="_blank"
      rel="noopener noreferrer"
      class="text-light-base0B dark:text-dark-base0B underline">CSE 291G</a
    >. See technical report <a
      href="./papers/rigid_body_simulation.pdf"
      target="_blank"
      rel="noopener noreferrer"
      class="text-light-base0B dark:text-dark-base0B underline">here</a
    >. See submission video <a
      href="https://youtu.be/cMOBy6q9Bds"
      target="_blank"
      rel="noopener noreferrer"
      class="text-light-base0B dark:text-dark-base0B underline">here</a
    >.
  </TagItemRow>

  <TagItemRow
    title="Fluid Dynamics Simulation"
    tags={["python", "simulation", "math"]}
  >
    A GPU-accelerated 2D fluid engine built in Python and Taichi to simulate the
    Karman vortex street phenomenon. Discretized the incompressible
    Navier-Stokes equations onto a Marker-and-Cell (MAC) grid, utilizing Strang
    Splitting and a Jacobi iterative solver for pressure projection. Implemented
    Semi-Lagrangian and Back-and-Forth Error Correction and Compensation (BFECC)
    schemes to minimize numerical diffusion during advection. Visualized flow
    patterns by advecting a passive scalar ink field and computing discrete
    vorticity. Built for <a
      href="#"
      target="_blank"
      rel="noopener noreferrer"
      class="text-light-base0B dark:text-dark-base0B underline">CSE 291G</a
    >. See technical report <a
      href="./papers/fluid_dynamics_simulation.pdf"
      target="_blank"
      rel="noopener noreferrer"
      class="text-light-base0B dark:text-dark-base0B underline">here</a
    >. See submission video <a
      href="https://youtu.be/cMOBy6q9Bds"
      target="_blank"
      rel="noopener noreferrer"
      class="text-light-base0B dark:text-dark-base0B underline">here</a
    >.
  </TagItemRow>

  <TagItemRow
    title="enclose.horse ILP Solver"
    link="https://github.com/pink10000/eh_ilp_solver"
    tags={["python", "algorithms", "math"]}
  >
    Built an Integer Linear Programming (ILP) solver for the puzzle game
    <a
      href="https://enclose.horse"
      target="_blank"
      rel="noopener noreferrer"
      class="text-light-base0B dark:text-dark-base0B underline">enclose.horse</a
    >, reducing each level to a multi-commodity flow ILP that enforces global
    connectivity (avoiding disconnected "adversarial" enclosures) while handling
    game mechanics like portals and tile modifiers. Benchmarked on 12,689
    puzzles, the solver matched the reported optimal score on 93.87% of levels
    and exceeded it on 5.33%, with a 0.354s median solve time on non-timeout
    cases. See technical report
    <a
      href="./papers/eh_ilp_solver.pdf"
      target="_blank"
      rel="noopener noreferrer"
      class="text-light-base0B dark:text-dark-base0B underline">here</a
    >.
  </TagItemRow>

  <TagItemRow title="Snek Compiler" tags={["rust", "compilers"]}>
    Developed a compiler for a Lisp-like language called Snek, targeting x86-64
    assembly in Rust. Implemented a full compiler pipeline including lexing,
    parsing, semantic analysis, and code generation. Supported features like
    branching, loops, first-class functions, closures, vectors, heap
    allocation/garbage collection, and register allocation optimization.
    Implemented as part of <a
      href="https://ucsd-cse231.github.io/sp26/index.html"
      target="_blank"
      rel="noopener noreferrer"
      class="text-light-base0B dark:text-dark-base0B underline">CSE 231</a
    >.
  </TagItemRow>

  <TagItemRow
    title="BlazeGuard.ai"
    link="https://devpost.com/software/blazeguard-ai"
    tags={["python", "machine learning"]}
  >
    Trained a ResNet 18-layer CNN on Canadian wildfire satellite images over the
    last 30 years to predict wildfire outbreaks. Won 2nd place @ UCSD
    DiamondHacks 2024.
  </TagItemRow>

  <TagItemRow
    title="Notes Website"
    link="https://kytrinh.me/notes/"
    tags={["markdown", "quartz"]}
  >
    Built and deployed a Quartz-based notes site to GitHub Pages, with CI
    workflows that automatically rebuild and publish updates on every push.
    Implemented custom Quartz plugins to support math-heavy content, including a
    Desmos transformer/emitter that integrates Obsidian Desmos graphs via
    pre-generated SVG assets, plus TikZ support for embedded diagram rendering.
  </TagItemRow>

  <TagItemRow
    title="Distributed ML Data Loading with gRPC"
    tags={["distributed systems", "python", "kubernetes"]}
  >
    Implemented a distributed data loading pipeline for
    <a
      href="https://www.cs.toronto.edu/~kriz/cifar.html"
      target="_blank"
      rel="noopener noreferrer"
      class="text-light-base0B dark:text-dark-base0B underline">CIFAR-100</a
    >
    backed by gRPC microservices with proxy caching and configurable prefetching to
    hide shard-latency. Added server-side data transformations selectable at runtime,
    and used a custom PyTorch CIFAR100 dataset to stream transformed shards over gRPC
    for training, with end-to-end deployment/testing on Kubernetes. See technical
    report
    <a
      href="./papers/grpc_data_loading.pdf"
      target="_blank"
      rel="noopener noreferrer"
      class="text-light-base0B dark:text-dark-base0B underline">here</a
    >.
  </TagItemRow>

  <TagItemRow title="Firefox Render Relay (F2R2)" tags={["rust", "systems"]}>
    Developed a high-fidelity, privacy-focused remote browsing architecture by
    streaming underlying WebRender drawing commands. F2R2 hooks directly into
    Firefox's graphics pipeline to relay rendering state to a thin client.
    Required extensive reverse-engineering of Mozilla's internal, Rust-based
    debugging tool
    <a
      href="https://github.com/mozilla-firefox/firefox/tree/main/gfx/wr/wrench"
      target="_blank"
      rel="noopener noreferrer"
      class="text-light-base0B dark:text-dark-base0B underline">wrench</a
    >. We developed caching mechanisms and restored incremental resource loading
    strategies, successfully resurrecting persistent text and dynamic image
    rendering. See technical report
    <a
      href="./papers/f2r2.pdf"
      target="_blank"
      rel="noopener noreferrer"
      class="text-light-base0B dark:text-dark-base0B underline">here</a
    >.
  </TagItemRow>

  <TagItemRow
    title="Auslander Buchsbaum Serre Theorem Formalization"
    link="https://github.com/UCSD-computational-number-theory/lean_abs"
    tags={["lean4", "math"]}
  >
    Attempted to formalize the Auslander-Buchsbaum-Serre theorem in Lean4, which
    characterizes regular local rings as those with finite global dimension.
    Project unfinished/canceled due to the complexity of the required
    homological algebra and commutative algebra machinery, alongside the steep
    learning curve of Lean4 and its mathlib library.
  </TagItemRow>

  <TagItemRow
    title="Concrete Semantics in Lean4"
    link="https://github.com/pink10000/concrete-semantics-lean"
    tags={["lean4", "math"]}
  >
    Formalized select chapters and problems of the textbook <a
      href="https://concrete-semantics.org/"
      target="_blank"
      rel="noopener noreferrer"
      class="text-light-base0B dark:text-dark-base0B underline"
      >Concrete Semantics</a
    > in Lean4, translating textbook operational semantics and program proofs into
    machine-checked code. Added a small DSL layer via custom notation (e.g., list
    constructors/operators) and used Mathlib tactics (e.g., linarith, ring) to mechanize
    and simplify proof workflows. AFAIK first of it's kind on GitHub.
  </TagItemRow>

  <TagItemRow title="Successerator Android App" tags={["java", "android"]}>
    Developed interactive UI elements for an Android To-Do app in an 6 person
    team with Agile Development. Used Java, Android Studio, JUnit, and Espresso
    (UI Testing). Built for CSE 110.
  </TagItemRow>

  <TagItemRow
    title="Capacitive Touch Keyboard Software"
    link="https://github.com/Grace-Note-LLC/GraceNote"
    tags={["python", "embedded"]}
  >
    Developed cross-compatible UI to program custom-made touchpads for rhythm
    games. Built with ESP32 S3 microcontroller, Flet (Python wrapper for
    Flutter), pyautogui, and pyserial. Awarded the Best Project Demo Award by
    IEEE out of 108 participants. IEEE QP FA23.
  </TagItemRow>

  <TagItemRow title="REST API Error Handling" tags={["python", "research"]}>
    Conducted research on REST API error handling, creating a Flask-based web
    coding environment to collect data on how software engineers handle errors
    to write code. Former research assistant at SALAD Lab @ UCSD under Professor
    Coblenz.
  </TagItemRow>
</ul>

<!--
<script>
  function initExpFilters() {
    const projectsList = document.getElementById("projectsList");
    if (!projectsList) return;
    const projectItems = Array.from(projectsList.querySelectorAll(".project-item"));
    const filterBtns = document.querySelectorAll(".filter-btn");
    const sortToggleBtn = document.getElementById("sortToggleBtn");
    const filterContainer = document.getElementById("filterButtonsContainer");

    let activeTag = "all";
    let activeSort = "default"; // 'default' or 'name'

    // Store the initial order of items for default sort
    const initialOrder = projectItems.map((item, index) => ({ item, index }));

    const activeClasses = ["active-filter", "bg-light-base02", "dark:bg-dark-base02", "text-light-base00", "dark:text-dark-base06", "border-transparent"];
    const inactiveClasses = ["bg-light-base01/40", "dark:bg-dark-base01/40", "text-light-base05", "dark:text-dark-base04", "hover:bg-light-base02/45", "dark:hover:bg-dark-base02/45", "border-light-base02/40", "dark:border-dark-base02/40"];

    // Helper to update visibility
    function updateFilter() {
      projectItems.forEach(item => {
        const tagsAttr = item.getAttribute("data-tags") || "";
        const tags = tagsAttr.split(",").map(t => t.trim());
        if (activeTag === "all" || tags.includes(activeTag)) {
          item.style.setProperty("display", "contents", "important");
        } else {
          item.style.setProperty("display", "none", "important");
        }
      });
    }

    // Helper to handle filter button styles
    function setActiveFilterButton(tag) {
      // Remove active styling from all existing buttons
      const allBtns = document.querySelectorAll(".filter-btn");
      allBtns.forEach(btn => {
        btn.classList.remove(...activeClasses);
        btn.classList.add(...inactiveClasses);
      });

      // Find if a button for this tag already exists
      let activeBtn = Array.from(allBtns).find(btn => btn.getAttribute("data-tag") === tag);

      // If it's a dynamic tag (e.g. "go" or "wasm"), it won't exist. Create it!
      if (!activeBtn && tag !== "all") {
        // Remove any previously created dynamic buttons
        const dynamicBtn = document.getElementById("dynamic-tag-filter");
        if (dynamicBtn) dynamicBtn.remove();

        // Create new one
        const btn = document.createElement("button");
        btn.id = "dynamic-tag-filter";
        btn.type = "button";
        btn.className = "filter-btn active-filter px-2 py-0.5 rounded font-mono transition-colors border cursor-pointer " + activeClasses.join(" ");
        btn.setAttribute("data-tag", tag);
        btn.textContent = tag;
        btn.addEventListener("click", () => handleTagSelect(tag));
        filterContainer.appendChild(btn);
        activeBtn = btn;
      } else if (tag === "all") {
        // Remove dynamic button if we go back to "all"
        const dynamicBtn = document.getElementById("dynamic-tag-filter");
        if (dynamicBtn) dynamicBtn.remove();
      }

      if (activeBtn) {
        activeBtn.classList.remove(...inactiveClasses);
        activeBtn.classList.add(...activeClasses);
      }
    }

    function handleTagSelect(tag) {
      activeTag = tag;
      setActiveFilterButton(tag);
      updateFilter();
    }

    // Click handler for top level filter buttons
    filterBtns.forEach(btn => {
      btn.addEventListener("click", () => {
        const tag = btn.getAttribute("data-tag");
        handleTagSelect(tag);
      });
    });

    // Click handler for tags inside the list (capturing badge clicks)
    document.addEventListener("click", (e) => {
      const tagBadge = (e.target as HTMLElement).closest(".tag-badge");
      if (tagBadge) {
        const tag = tagBadge.getAttribute("data-tag");
        if (tag) {
          handleTagSelect(tag);
          // Scroll to the top of the projects section smoothly so user sees the change
          const filterUI = document.getElementById("filterButtonsContainer");
          if (filterUI) {
            filterUI.scrollIntoView({ behavior: "smooth", block: "nearest" });
          }
        }
      }
    });

    // Sorting logic
    function updateSort() {
      if (activeSort === "default") {
        // Re-order DOM based on initialOrder
        initialOrder.forEach(({ item }) => {
          projectsList.appendChild(item);
        });
        if (sortToggleBtn) sortToggleBtn.textContent = "default";
      } else if (activeSort === "name") {
        // Sort alphabetically by data-title
        const sorted = [...projectItems].sort((a, b) => {
          const titleA = a.getAttribute("data-title") || "";
          const titleB = b.getAttribute("data-title") || "";
          return titleA.localeCompare(titleB);
        });
        sorted.forEach(item => {
          projectsList.appendChild(item);
        });
        if (sortToggleBtn) sortToggleBtn.textContent = "alphabetical";
      }
    }

    if (sortToggleBtn) {
      sortToggleBtn.addEventListener("click", () => {
        if (activeSort === "default") {
          activeSort = "name";
        } else {
          activeSort = "default";
        }
        updateSort();
      });
    }
  }

  // Initialize immediately
  initExpFilters();

  // Support Astro View Transitions if applicable
  document.addEventListener("astro:page-load", initExpFilters);
</script>
-->
